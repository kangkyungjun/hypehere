# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
android/.gradle/
ios/Pods/

# ============================================
# SECRETS & CREDENTIALS (DO NOT COMMIT)
# ============================================

# Centralized secrets directory
secrets/

# Signing keys
*.jks
*.keystore
*.p8
*.p12
*.pem
*.key
*.mobileprovision
*.cer
/android/key.properties

# Environment variables
.env
.env.*
!.env.example

# Google Cloud / Firebase service account keys
gen-lang-client-*.json
*-service-account*.json
firebase-*.json
!firebase.json
# GCP service account key pattern: <project-id>-<12hex>.json
marketlens-*.json

# Firebase config files containing API keys
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Fastlane credentials
fastlane/api_key.json

# Subscription / Auth keys
AuthKey_*.p8
SubscriptionKey_*.p8

# Databases
*.db
*.sqlite3
# lib/preview_harness.dart — 추적 대상으로 전환(2026-08-30).
# 배포 진입점이 아니라 별도 -t 타깃이라 앱 번들에 들어가지 않는다.
# 로그인·네트워크 없이 티커상세를 렌더해 디자인 회귀를 눈으로 잡는 도구이므로
# 커밋해서 재현 가능하게 둔다.

# 골든 테스트 실패 산출물 (재생성 대상, 커밋 금지)
test/failures/
