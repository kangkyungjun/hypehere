# Django Environment
DJANGO_ENV=production

# Django Secret Key (Generate a new one: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
SECRET_KEY=your-secret-key-here-change-this

# Allowed Hosts (comma separated, e.g., yourdomain.com,www.yourdomain.com,your-ec2-ip)
ALLOWED_HOSTS=your-domain.com

# Database Configuration
DB_NAME=contactotalk
DB_USER=contactotalk
DB_PASSWORD=your-database-password-here
DB_HOST=localhost
DB_PORT=5432

# Redis Configuration
REDIS_URL=redis://127.0.0.1:6379/0

# CORS Origins (comma separated, e.g., https://yourdomain.com,https://www.yourdomain.com)
CORS_ALLOWED_ORIGINS=https://your-domain.com

# SSL Configuration (set to False if not using HTTPS initially)
SECURE_SSL_REDIRECT=True

# Frontend URL (for Next.js)
NEXT_PUBLIC_API_URL=https://your-domain.com/api
