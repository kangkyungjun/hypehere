# ==================== Django Configuration ====================

# Debug mode (set to False in production)
DEBUG=False

# Django secret key (generate new one for production)
# Generate using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-secret-key-here-generate-new-one-using-python-command

# Allowed hosts (comma-separated domains)
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com,www.yourdomain.com

# ==================== Site Configuration ====================

# Site URL for email links and redirects (use HTTPS in production)
SITE_URL=https://yourdomain.com

# ==================== Email Configuration ====================

# Gmail SMTP configuration
# For Gmail, generate App Password at: https://myaccount.google.com/apppasswords
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password

# ==================== Security Configuration ====================

# CSRF trusted origins (comma-separated, HTTPS only in production)
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==================== Database Configuration (for production - Phase 2) ====================

# PostgreSQL database URL (required when DEBUG=False)
# Format: postgresql://user:password@host:5432/dbname
# AWS RDS Example: postgresql://hypehere_user:your_password@hypehere-db.xxxxx.us-east-1.rds.amazonaws.com:5432/hypehere
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ==================== Redis Configuration (for production - Phase 2) ====================

# Redis URL for Channel Layers (WebSocket) (required when DEBUG=False)
# Format: redis://host:6379/1
# AWS ElastiCache Example: redis://hypehere-redis.xxxxx.cache.amazonaws.com:6379/1
REDIS_URL=redis://host:6379/1

# ==================== AWS Configuration (for production - Phase 3) ====================

# S3 bucket for static and media files (required when DEBUG=False)
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1

# AWS credentials (optional if using IAM roles on EC2/ECS)
# For better security, use IAM roles instead of access keys when deploying to AWS
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# ==================== Additional Production Settings ====================

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Celery broker URL for background tasks (optional)
# CELERY_BROKER_URL=redis://host:6379/0
# CELERY_RESULT_BACKEND=redis://host:6379/0
